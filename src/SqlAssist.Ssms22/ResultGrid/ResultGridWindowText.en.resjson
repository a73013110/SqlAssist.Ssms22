{
  "ShapeWholeResult": "{action}: {columns} × {rows} (columns × rows, entire result).",
  "ShapeSelection": "{action}: {columns} × {rows} (columns × rows, selection).",
  "QueryWindowNotBlank": "The new query window isn't empty, so the script wasn't written.",
  "Copied": "Copied {what}",
  "CopyWhatFailed": "Couldn't copy {what}: {message}",
  "InPredicate": "IN predicate",
  "MarkdownTable": "Markdown table",
  "NothingToProfile": "The selection has no rows to profile.",
  "NoQueryWindow": "No active query window was found.",
  "NoResultGrid": "This query window has no results grid. Run a query that returns results first.",
  "GridHasNoData": "This results grid has no data yet.",
  "CellReaderMissing": "This version of SSMS provides no way to read cells, so values can't be taken from the results grid.",
  "ClickCellFirst": "Click a cell in the results grid, then run this command again.",
  "CellOutOfRange": "The clicked position is outside the data.",
  "ColumnsReordered": "Columns in this results grid were reordered. SqlAssist reads values only in the original order; restore the column order (or run the query again) and try again.",
  "CellWindowTitle": "SqlAssist — Cell content",
  "WordWrap": "Word wrap",
  "WordWrapTip": "Changes only the display; copying keeps the original line breaks and whitespace.",
  "CellContentName": "Full cell content (read-only)",
  "CellIsNull": "NULL — this cell has no value",
  "CellIsEmpty": "Empty — length 0",
  "CopyAll": "Copy all",
  "CopyAllTip": "Copy the full original text. You can also select part of it and press Ctrl+C.",
  "CellCopied": "Copied the full content of this cell.",
  "ProfileWindowTitle": "SqlAssist — Column profile",
  "ProfileSummaryWholeResult": "{columns:N0} × {rows:N0} (columns × rows) · Entire result · Counts only the loaded results; the database is not queried again",
  "ProfileSummarySelection": "{columns:N0} × {rows:N0} (columns × rows) · Selection · Counts only the loaded results; the database is not queried again",
  "NoMatchingColumns": "No matching columns",
  "CopyTable": "Copy current table",
  "CopyTableTip": "Copy the columns as currently filtered and sorted, with headers, as a table you can paste into Excel.",
  "AllColumns": "All columns",
  "AllNull": "All NULL",
  "SingleValue": "Single value",
  "KindFilterTip": "\"Single value\" needs at least two rows, and NULL counts as a value. Only the current result is checked.",
  "KindFilterName": "Column state filter",
  "SearchName": "Filter by column name or type",
  "Filter": "Filter",
  "SearchTip": "Type a column name or data type; clear it to show all.",
  "ColumnCount": "Columns: {shown:N0} / {total:N0}",
  "ProfileGridName": "Column statistics; select a header to sort",
  "NullCount": "NULL count",
  "EmptyTextCount": "Empty strings",
  "DistinctCount": "Distinct values",
  "DistinctCountTip": "NULL counts as a value; this differs from SQL COUNT(DISTINCT).",
  "TextLength": "Length range",
  "TextLengthTip": "Shortest to longest text length in characters; empty for non-text columns or when nothing can be counted.",
  "Minimum": "Minimum",
  "Maximum": "Maximum",
  "MinMaxTip": "T-SQL literal; empty when there is nothing to compare. Sorted by display text, not by SQL type.",
  "ProfileCopied": "Copied the column summary. Columns: {count}."
}
