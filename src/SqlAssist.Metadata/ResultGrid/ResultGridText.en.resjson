{
  "CellNoColumnName": "(no column name)",
  "CellSummary": "{name} ({type}) · {detail}",
  "CellCharacters": "Characters: {count:N0}",
  "CellBytes": "Bytes: {count:N0}",
  "SourceWholeResult": "Generated by SqlAssist from query results: {columns} × {rows} (columns × rows, entire result).",
  "SourceSelection": "Generated by SqlAssist from query results: {columns} × {rows} (columns × rows, selection).",
  "ValueNotLiteral": "Column \"{column}\" in row {row} can't be converted to a T-SQL literal: {reason}.",
  "NoRows": "This result has no rows to take.",
  "SelectionTooLarge": "The selection is {columns} × {rows} (columns × rows), {cells} cells in total, which exceeds the limit of {limit} cells at a time. Narrow the selection first.",
  "InUnavailable": "Couldn't create an IN predicate from the query results.",
  "JsonUnavailable": "Couldn't create JSON from the query results.",
  "MarkdownUnavailable": "Couldn't create a Markdown table from the query results.",
  "TempTableUnavailable": "Couldn't create a temporary table script from the query results.",
  "EmptySelection": "The selection has no rows, or the result has no columns.",
  "SelectCellsFirst": "Select some cells in the results grid, then run this command again.",
  "SelectKeyColumnsFirst": "Select the column (or columns) to filter on in the results grid, then run this command again.",
  "RemoveFailedColumn": "Remove that column from the selection and try again; all other columns can be converted.",
  "InNullAdded": "The selection contains NULL. An IN list never matches NULL, so it is added with OR.",
  "InCompositeKey": "SQL Server doesn't accept row values such as (a, b) IN ((1, N'x')), so the composite key is written as OR conditions.",
  "InUnqualifiedNames": "Column names have no alias; qualify them yourself in multi-table queries.",
  "TempTableNoRequery": "Values come from data the results grid already fetched; the database is not queried again.",
  "TempTableNoServerType": "The server type of column \"{column}\" is unavailable, and CREATE TABLE can't be written without it. Expression columns sometimes have no type; giving the column an alias and running the query again usually fixes this.",
  "LiteralUnsupportedClrType": "CLR type {type} has no matching T-SQL literal form",
  "LiteralUnexpectedNumber": "the formatted number contains unexpected characters and can't be written safely as a literal",
  "LiteralTimeOutOfRange": "the time value is outside the time type's range of 0:00:00 to 23:59:59.9999999"
}
